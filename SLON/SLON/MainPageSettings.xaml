<?xml version="1.0" encoding="utf-8" ?>
<mct:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SLON.MainPageSettings"
             xmlns:mct="clr-namespace:CommunityToolkit.Maui.Views;assembly=CommunityToolkit.Maui"
             xmlns:ui="clr-namespace:Xe.AcrylicView;assembly=Xe.AcrylicView"
             Color="Transparent"
           CanBeDismissedByTappingOutsideOfPopup="False">

    <Border Stroke="Transparent" BackgroundColor="#737373" Padding="20" WidthRequest="300">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="20" />
        </Border.StrokeShape>

        <VerticalStackLayout x:Name="verticalStackLayout" Spacing="15">
            <!-- Заголовок -->
            <Label Text="Select the desired categories"
                   FontSize="18"
                   HorizontalOptions="Center"
                   TextColor="Black"
                   FontAttributes="Bold"/>

            <!-- Кнопка закрытия -->
            <Button Text="Close"
                    BackgroundColor="Black"
                    TextColor="White"
                    CornerRadius="10"
                    WidthRequest="140"
                    FontAttributes="Bold"
                    Clicked="OnCloseClicked" />
        </VerticalStackLayout>
    </Border>
</mct:Popup>
